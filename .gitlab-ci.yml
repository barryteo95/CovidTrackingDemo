image: openjdk:8

before_script:
    - apt-get update
    - apt-get -y install ant

stages:
    - build
    - test

build:
    stage: build
    script: 
        - cd CovidTrackingDemo
        - ant compile

test:
    stage: test
    script: 
        - cd CovidTrackingDemo
        - ant runtest
