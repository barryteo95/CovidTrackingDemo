image: docker:latest

services:
    - docker:dind

before_script:
    - docker run --rm -i ubuntu bash
    - apk update 
    - apk add ant
    
build:
    stage: build
    script:
        - ant -version
