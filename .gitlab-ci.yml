image: openjdk:14

stages:
    - build
    - test

build:
    stage: build
    script: 
        - cd CovidTrackingDemo
        - ant compile

test:
    stage: test
    script: 
        - cd CovidTrackingDemo
        - ant runtest
