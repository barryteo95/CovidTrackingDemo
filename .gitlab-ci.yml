image: docker:latest

services:
    - docker:dind

before_script:
    - docker run --rm -i ubuntu bash
    - apt-get update 
    - apt-get install ant
    
build:
    stage: build
    script:
        - ant -version
